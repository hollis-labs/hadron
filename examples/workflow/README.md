# Graph-Native Workflow Examples

This directory is reserved for graph-native `workflow` examples introduced by
later implementation tasks. No target-format example is available yet; the
files under [`../archive/legacy-blueprints-pipelines/`](../archive/legacy-blueprints-pipelines/)
are rewrite references only.
