workflow:
  id: release
  version: v1

steps:
  - id: build
    kind: command
    outputs:
      - name: version
        schema:
          type: string

  - id: deploy
    kind: command
    needs: [approve]
    with:
      build_version:
        expression: steps.build.outputs.version
