{
  "name": "checkpoint-v1-contract",
  "expect": "pass",
  "input": {
    "spec": {
      "name": "checkpoint",
      "version": "v1",
      "config_schema": {},
      "input_schema": {},
      "output_schema": {},
      "effects": ["read"],
      "required_capabilities": ["checkpoint.respond", "wait.resume"],
      "idempotency": "intrinsic",
      "retry_safety": "safe",
      "cancellation": {"mode": "context"},
      "observation": {"mode": "none"},
	  "memoization": "disabled",
      "can_suspend": true
    }
  }
}
