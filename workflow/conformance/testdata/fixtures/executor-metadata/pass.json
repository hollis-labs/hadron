{
  "name": "minimal-pass",
  "expect": "pass",
  "input": {
    "spec": {
      "name": "noop",
      "version": "v1",
      "config_schema": {},
      "input_schema": {},
      "output_schema": {},
      "effects": ["compute"],
      "required_capabilities": ["embedded"],
      "idempotency": "intrinsic",
      "retry_safety": "safe",
      "cancellation": {"mode": "context"},
      "observation": {"mode": "none"},
      "embedded_mode_supported": true
    }
  }
}
