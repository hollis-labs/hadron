{
  "name": "timeout-catch",
  "expect": "pass",
  "input": {
    "scenario": "catch",
    "status": "timed_out",
    "failure_code": "execution_timeout",
    "timeout": "execution",
    "catch": [{"errors": ["execution"], "targets": ["timeout-handler"]}],
    "want_outcome": "selected",
    "want_targets": ["timeout-handler"]
  }
}
