// Package service implements the provider-neutral service@v1 step kind.
// Provider process/container identities and handles remain behind Host; the
// adapter exports only bounded opaque references and typed readiness outputs.
package service
