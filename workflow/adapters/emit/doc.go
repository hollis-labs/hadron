// Package emit implements emit@v1 as an extraction-ready, host-bound event
// publication step kind. Transport selection, destination authorization, and
// durable publication idempotency remain host responsibilities.
package emit
