---
id: TASK-HAD-20260228-004
title: "persistence package — SQLite store with migrations"
status: done
priority: A
project: hadron
sprint_id: sprint-HAD-03a
created_at: "2026-02-28"
updated_at: "2026-02-28"
tags: [foundation, persistence, sqlite]
depends_on: [TASK-HAD-20260228-001]
---

## Description

Port the SQLite persistence layer from vnext. This package is architecturally sound and well-tested. Port it with module path rename only — do not restructure.

## Instructions

- Copy `vnext-blueprint-runner/internal/persistence/store.go` → `hadron/internal/persistence/store.go`
- Copy `vnext-blueprint-runner/internal/persistence/migrate.go` → `hadron/internal/persistence/migrate.go`
- Copy `vnext-blueprint-runner/internal/persistence/store_test.go` → `hadron/internal/persistence/store_test.go`
- Update all import paths: `hollis-labs/cortex` → `hollis-labs/hadron`
- Add `github.com/mattn/go-sqlite3` to `go.mod`
- Enable WAL mode on open: add `PRAGMA journal_mode=WAL; PRAGMA synchronous=NORMAL;` to the `Open()` function after ping
- Verify `go test ./internal/persistence/...` passes

## Acceptance

- `go test ./internal/persistence/...` passes
- `Open(path)` creates DB, runs all migrations, returns Store
- Can CRUD: workspaces, runs, run_events, schedules, pipeline_runs, pipeline_stage_runs
- Cursor-based pagination works on `ListRunsByWorkspaceFiltered`
- `ClaimAndUpdateScheduleRun` is atomic (prevents double-fire)
- WAL mode enabled

## Source Material

- `vnext-blueprint-runner/internal/persistence/store.go` (port as-is, rename module)
- `vnext-blueprint-runner/internal/persistence/migrate.go` (port as-is, rename module)
- `vnext-blueprint-runner/internal/persistence/store_test.go` (port as-is, rename module)
