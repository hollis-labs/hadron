---
id: TASK-HAD-20260228-002
title: "specparse package — YAML/JSONC parser"
status: done
priority: A
project: hadron
sprint_id: sprint-HAD-03a
created_at: "2026-02-28"
updated_at: "2026-02-28"
tags: [foundation, parser, specparse]
depends_on: [TASK-HAD-20260228-001]
---

## Description

Port the `specparse` package from vnext. This is a clean, tested YAML/JSONC parser with comment stripping and trailing comma removal. Port as-is — it has no bugs and already has tests.

## Instructions

- Copy `vnext-blueprint-runner/internal/specparse/specparse.go` → `hadron/internal/specparse/specparse.go`
- Copy `vnext-blueprint-runner/internal/specparse/specparse_test.go` → `hadron/internal/specparse/specparse_test.go`
- Update package imports to use `github.com/hollis-labs/hadron/internal/specparse`
- Verify `go test ./internal/specparse/...` passes with no changes needed

## Acceptance

- `go test ./internal/specparse/...` passes
- Correctly parses YAML input
- Correctly parses JSONC (strips `//` and `/* */` comments, removes trailing commas)
- Format detection works by file extension and content heuristics

## Source Material

- `vnext-blueprint-runner/internal/specparse/specparse.go` (port as-is)
- `vnext-blueprint-runner/internal/specparse/specparse_test.go` (port as-is)
