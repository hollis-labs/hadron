---
id: TASK-HAD-20260228-001
title: "Go module skeleton + project layout"
status: done
priority: A
project: hadron
sprint_id: sprint-HAD-03a
created_at: "2026-02-28"
updated_at: "2026-02-28"
tags: [foundation, module, skeleton]
---

## Description

Initialize the Hadron Go module and establish the canonical directory structure for Phase 1.

## Instructions

- Create `hadron/go.mod` with module path `github.com/hollis-labs/hadron`
- Create stub `main.go` files in `cmd/hadrond/` and `cmd/hadron/` (just `package main` + empty `func main()`)
- Create empty package stubs for all internal packages: `internal/specparse`, `internal/blueprint`, `internal/config`, `internal/persistence`, `internal/execution`, `internal/scheduler`, `internal/pipeline`, `internal/api`, `internal/mcpadapter`, `internal/settings`
- Add `.gitignore` (Go standard: binaries, vendor/, .DS_Store)
- Add `Makefile` with targets: `build`, `test`, `lint`, `run-daemon`
- Add minimal `README.md` (name, one-liner, build instructions)

## Acceptance

- `cd hadron && go build ./...` passes (all stubs compile)
- Directory structure matches `artifacts/plan/02-architecture.md`
- Module path is `github.com/hollis-labs/hadron`

## Source Material

- Architecture: `artifacts/plan/02-architecture.md`
- No source porting needed for this task
